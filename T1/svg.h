#ifndef __SVG_H__
#define __SVG_H__

/* está funcao cria a tag de abertura de svg no arquivo,
ela recebe o local de saida como parametro*/
void abrirSvg(char saida[]);

/* está funcao cria a tag de fechamento de svg no arquivo,
ela recebe o local de saida como parametro*/
void fecharSvg(char saida[]);

#endif